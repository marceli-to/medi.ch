# Laravel
/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.nova
/.vscode
/.zed

# macOS
.DS_Store

# Logs
*.log

# Claude Code local settings
/.claude/settings.local.json
/.claude/scheduled_tasks.lock

# Vite dev-only fonts manifest (the build emits public/build/fonts-manifest.json)
/public/fonts-manifest.dev.json

# Source material (PDF, prompts). Ignore the .data dir, but TRACK the
# medicament markdown under .data/medikamente/ — the importer
# (app:import-medicaments) reads it from base_path('.data/medikamente').
/.data/*
!/.data/medikamente/
/.data/medikamente/.DS_Store
INSTALL.txt
